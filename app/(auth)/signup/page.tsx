import { AuthScreen } from '@acceso/usuarios/UI/AuthScreen';

export const metadata = { title: 'Create workspace · waHub' };

export default function SignupPage() {
  return <AuthScreen mode="signup" />;
}
